version: "3.3"
services:
  db:
    image: postgres
    env_file: ./.env
    ports: 
      - "5432:5432"
  web:
    build: .
    image: server
    volumes: 
      - .:/transcendence_app/
    depends_on: 
      - db
    ports:
      - "3000:3000"