version: "3.9"
services:
  db:
    image: postgres
    volumes:
      - ./tmp/db:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: password
  web:
    build: .
    image: server
    volumes: 
      - .:/transcendence_app/
    depends_on: 
      - db
    ports:
      - "3000:3000"